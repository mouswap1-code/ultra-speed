worker_processes auto;

events {
    worker_connections 1024;
}

stream {
    upstream backend {
        server 188.213.28.174:80;
    }
    
    server {
        listen 8080;
        proxy_pass backend;
        proxy_connect_timeout 30s;
        proxy_timeout 30m;
        tcp_nodelay on;
    }
}
